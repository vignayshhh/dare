// Middleware/Application Layer
// This layer contains services that orchestrate between frontend and backend
// Clean interfaces, DTOs, business workflows
// No infrastructure dependencies, no UI dependencies

export * from './services';
